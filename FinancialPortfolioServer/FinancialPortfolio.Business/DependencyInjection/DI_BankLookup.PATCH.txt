// MERGE into your Business DI registration method (AddBusinessLayer / similar)
// next to MutualFundNavService / WealthTrackerService registration:

services.AddHttpClient<IBankLookupService, BankLookupService>(client =>
{
    client.BaseAddress = new Uri("https://ifsc.razorpay.com/");
    client.Timeout = TimeSpan.FromSeconds(8);
});

// If validators are registered via assembly scan, PayRdInstallmentRequestValidator
// and UpsertRdInstallmentRequestValidator will be picked up automatically.
// Otherwise register:
// services.AddValidatorsFromAssemblyContaining<PayRdInstallmentRequestValidator>();
